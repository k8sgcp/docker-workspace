name: .NET Outer Loop CI Pipeline

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore Dependencies
        run: dotnet restore dotnet-app/dotnet-app.csproj

      - name: Build Solution
        run: dotnet build dotnet-app/dotnet-app.csproj --no-restore -c Release

      - name: Build Multi-Stage Production Docker Image
        run: |
          docker build -t dotnet-service:prod -f dotnet-app/Dockerfile dotnet-app/

      - name: Run Trivy Security Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'dotnet-service:prod'
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
